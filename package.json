{
  "name": "klyde-illustration-portfolio",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name klyde-illustration-portfolio"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}